autowire -> @Autowired
bean -> @Component
placeholder -> @Value
beans.xml -> @Configuration
component scan -> @ComponentScan(basePackage = "com")


Component -> scanned

autowiring:
byType -> by using Class Name
byName -> field name and bead id name should match
constructor -> it expects a param constructor

@Autowired
    field
    setter
    constructor

    Employee constructor
    address bean @Autowired

    Employee bean -> empty bean
    null


@Component

@Value("${server.port}")
@PropertySource("")

@ComponentScan -> config class 
@Configuration 


@Configuration
@ComponentScan(basePackages = "com")
@PropertySource("classpath:application.properties")
public class Config {

}


@Qualifier
@Primary



BEAN LIFE CYCLE:
1. Container is started
2. Beans are initialised
3. Dependencies Injected
4. init ( @PostConstruct )
5. normal methods
6. destroy (@PreDestroy)


Scope of Beans:
    1. Singleton
    2. Prototype
    3. Request
    4. Session
    5. application


Steps in JDBC:
1. Register JDBC
2. Create Connection
3. Create Statement
4. Prepare Query
5. Execute Query
6. Close Connection


Spring JDBC: 
It creates connection.
It creates Statement
It Execute
It will close connection automatically
It handles checked exception and converts it into unchecked exception.

* SpringJDBC provides JdbcTemplate -> bean 
    DataSource -> url username password.
    DriverManagerDataSource
We have to create the bean for JdbcTemplate.
@Component 
@Bean 

when the class is our custom -> @Component 
when the class is not our custom class -> @Bean 

